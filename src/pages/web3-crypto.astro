---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import CTABanner from '../components/CTABanner.astro';

// PAGE: Web3 & Blockchain Development
// TODO: Replace placeholder case study with real client data when available
---

<Layout
  title="Web3 & Blockchain Development — All Webbed Labs | Smart Contracts Australia"
  description="Smart contract development, DeFi platforms and NFT marketplaces. Ethereum, Solana, Polygon and more. Enterprise blockchain solutions built for Australia."
>
  <Header />
  <main>

    <!-- ═══════════════════════════════════════════════════
         HERO
    ═══════════════════════════════════════════════════ -->
    <section class="w3-hero">
      <div class="w3-hero-bg" aria-hidden="true">
        <div class="w3-glow-1"></div>
        <div class="w3-glow-2"></div>
        <div class="w3-grid"></div>
      </div>
      <div class="w3-hero-inner">
        <div class="w3-hero-content reveal">
          <nav class="breadcrumb" aria-label="Breadcrumb">
            <a href="/">Home</a><span>/</span><span>Web3 &amp; Blockchain</span>
          </nav>
          <span class="section-label">Web3 &amp; Blockchain Development</span>
          <h1 class="w3-hero-title">
            Build The Future of<br />
            <span class="gradient-text">Decentralised Technology</span>
          </h1>
          <p class="w3-hero-sub">
            We architect and deploy production-grade blockchain systems — smart contracts that don't get hacked, DeFi protocols
            that handle real liquidity, and NFT platforms built to scale. Rigorous security audits included. No shortcuts.
          </p>
          <div class="w3-hero-ctas">
            <a href="/contact" class="btn-primary">
              Book a Web3 Discovery Call
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
            </a>
            <a href="#services" class="btn-outline">View Services</a>
          </div>
          <div class="hero-trust-row">
            <div class="trust-item">
              <span class="trust-num">$0</span>
              <span class="trust-label">Smart Contract Exploits to Date</span>
            </div>
            <div class="trust-divider"></div>
            <div class="trust-item">
              <span class="trust-num">10+</span>
              <span class="trust-label">Chains Supported</span>
            </div>
            <div class="trust-divider"></div>
            <div class="trust-item">
              <span class="trust-num">100%</span>
              <span class="trust-label">Audited Before Launch</span>
            </div>
          </div>
        </div>
        <div class="w3-hero-visual reveal-right stagger-2" aria-hidden="true">
          <div class="chain-vis">
            <div class="chain-block cb-1">Block</div>
            <div class="chain-arrow">→</div>
            <div class="chain-block cb-2">Block</div>
            <div class="chain-arrow">→</div>
            <div class="chain-block cb-3">Block</div>
          </div>
          <div class="chain-hash">0x4a3f…b8c2</div>
        </div>
      </div>
    </section>

    <!-- ═══════════════════════════════════════════════════
         SERVICES GRID
    ═══════════════════════════════════════════════════ -->
    <section class="w3-section bg-surface" id="services">
      <div class="w3-inner">
        <div class="section-header reveal">
          <span class="section-label">What We Build</span>
          <h2 class="section-heading">Full-Stack Blockchain Development</h2>
          <p class="section-subheading">
            From protocol-level smart contracts to polished end-user interfaces — we cover every layer of the Web3 stack.
          </p>
        </div>
        <div class="w3-services-grid">
          {[
            {
              icon: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/></svg>`,
              title: 'Smart Contract Development',
              desc: 'Production-grade Solidity (EVM chains) and Rust (Solana, Near) contracts. Architecture review, gas optimisation and comprehensive test suites before a single line touches mainnet.'
            },
            {
              icon: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>`,
              title: 'DeFi Platform Development',
              desc: 'AMMs, lending protocols, yield aggregators, staking contracts and liquidity management systems. We\'ve built DeFi infrastructure handling eight-figure TVL without incident.'
            },
            {
              icon: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2" ry="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>`,
              title: 'NFT Marketplaces & Platforms',
              desc: 'ERC-721, ERC-1155 and programmable NFT contracts with royalty enforcement, lazy minting, marketplace aggregation and custom storefront development for creators and enterprises.'
            },
            {
              icon: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>`,
              title: 'DAO Governance Systems',
              desc: 'On-chain governance contracts with configurable voting mechanisms (token-weighted, quadratic, conviction), proposal frameworks, multi-sig treasury management and Snapshot integration.'
            },
            {
              icon: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="5" width="20" height="14" rx="2"/><line x1="2" y1="10" x2="22" y2="10"/></svg>`,
              title: 'Crypto Wallets & Custody',
              desc: 'Non-custodial wallet applications, MPC key management, hardware wallet integrations and enterprise custody solutions. Built to the highest security standards with full key recovery paths.'
            },
            {
              icon: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>`,
              title: 'Token Creation & Tokenomics',
              desc: 'ERC-20/SPL token design and deployment, vesting schedules, token distribution contracts, bonding curves and tokenomics modelling that accounts for game theory and long-term sustainability.'
            },
            {
              icon: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>`,
              title: 'Enterprise Blockchain',
              desc: 'Private and consortium blockchain deployments on Hyperledger Fabric, Quorum and permissioned EVM chains. Supply chain provenance, document notarisation and regulated asset registries.'
            },
            {
              icon: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="17 1 21 5 17 9"/><path d="M3 11V9a4 4 0 0 1 4-4h14"/><polyline points="7 23 3 19 7 15"/><path d="M21 13v2a4 4 0 0 1-4 4H3"/></svg>`,
              title: 'Cross-Chain Bridges',
              desc: 'Secure bridge infrastructure for asset transfers between L1s and L2s using proven protocols. Formal verification of bridge logic and staged deployment with circuit breakers and emergency pauses.'
            },
            {
              icon: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>`,
              title: 'Layer 2 Solutions',
              desc: 'Optimistic rollup and ZK-rollup deployments, Arbitrum/Optimism contract deployment, StarkNet application development and custom L2 infrastructure for high-throughput enterprise use cases.'
            },
            {
              icon: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.07 4.93a10 10 0 0 1 0 14.14M4.93 4.93a10 10 0 0 0 0 14.14"/></svg>`,
              title: 'Web3 Consulting & Strategy',
              desc: 'Independent advice on protocol selection, tokenomics design, regulatory exposure, integration architecture and launch strategy. No vendor bias — we tell you what will actually work for your use case.'
            },
          ].map((svc, i) => (
            <div class={`w3-svc-card card-hover reveal stagger-${Math.min((i % 6) + 1, 6)}`}>
              <div class="w3-svc-icon" set:html={svc.icon} />
              <h3>{svc.title}</h3>
              <p>{svc.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- ═══════════════════════════════════════════════════
         BLOCKCHAINS SUPPORTED
    ═══════════════════════════════════════════════════ -->
    <section class="w3-section bg-dark">
      <div class="w3-inner">
        <div class="section-header reveal">
          <span class="section-label">Chain Support</span>
          <h2 class="section-heading">Blockchains We Build On</h2>
          <p class="section-subheading">
            Multi-chain expertise across every major L1 and L2 ecosystem. We match the chain to the use case, not the other way around.
          </p>
        </div>
        <div class="chains-grid">
          {[
            { name: 'Ethereum', note: 'EVM — L1 flagship, institutional DeFi', colour: '#627eea' },
            { name: 'Solana', note: 'High throughput — 65,000 TPS, sub-cent fees', colour: '#9945ff' },
            { name: 'Polygon', note: 'EVM — scalable, low cost, enterprise adoption', colour: '#8247e5' },
            { name: 'Base', note: 'Coinbase L2 — OP Stack, regulatory-friendly', colour: '#0052ff' },
            { name: 'Avalanche', note: 'Subnet architecture — custom chain deployments', colour: '#e84142' },
            { name: 'BNB Chain', note: 'EVM — high volume, Southeast Asia reach', colour: '#f0b90b' },
            { name: 'Arbitrum', note: 'Optimistic rollup — Ethereum security + speed', colour: '#28a0f0' },
            { name: 'Optimism', note: 'OP Stack — widely adopted L2 ecosystem', colour: '#ff0420' },
            { name: 'Sui', note: 'Move VM — parallel execution, object model', colour: '#6fbcf0' },
            { name: 'Aptos', note: 'Move VM — block-STM parallelism, enterprise', colour: '#00d4aa' },
          ].map((chain, i) => (
            <div class={`chain-badge reveal stagger-${Math.min((i % 6) + 1, 6)}`}>
              <div class="chain-dot" style={`background:${chain.colour}`}></div>
              <div>
                <div class="chain-name">{chain.name}</div>
                <div class="chain-note">{chain.note}</div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- ═══════════════════════════════════════════════════
         USE CASES
    ═══════════════════════════════════════════════════ -->
    <section class="w3-section bg-surface">
      <div class="w3-inner">
        <div class="section-header reveal">
          <span class="section-label">Real-World Applications</span>
          <h2 class="section-heading">Web3 Solutions That Operate in the Real Economy</h2>
          <p class="section-subheading">
            Blockchain works when it solves a problem that centralised systems genuinely can't — here are the ones worth building.
          </p>
        </div>
        <div class="usecases-grid">
          {[
            {
              category: 'DeFi',
              title: 'Decentralised Lending & Borrowing',
              desc: 'Protocol-level collateralised lending with dynamic interest rate models, liquidation engines and multi-asset collateral. Designed for capital efficiency and stress-tested against flash loan attack vectors.'
            },
            {
              category: 'Digital Assets',
              title: 'NFT Marketplace with Royalty Enforcement',
              desc: 'Primary and secondary market platform with EIP-2981 royalty standards enforced on-chain, creator dashboards, batch minting tooling and IPFS/Arweave metadata pinning.'
            },
            {
              category: 'Governance',
              title: 'DAO Voting & Treasury Management',
              desc: 'On-chain governance for protocol decisions with time-locked execution, multi-sig treasury controls, delegated voting and a governance forum integration bridging off-chain discussion to on-chain action.'
            },
            {
              category: 'Tokenisation',
              title: 'Real-World Asset (RWA) Tokenisation',
              desc: 'Fractional ownership of real estate, infrastructure assets and private credit instruments — with compliant transfer restrictions, investor accreditation gating and automated dividend distribution.'
            },
            {
              category: 'Payments',
              title: 'Crypto Payment Gateway',
              desc: 'Multi-chain payment acceptance with instant fiat settlement via on/off-ramp integrations. Supports stablecoins (USDC, USDT) and native tokens with merchant dashboard and ERP reconciliation.'
            },
            {
              category: 'Supply Chain',
              title: 'Blockchain Supply Chain Provenance',
              desc: 'Immutable product provenance tracking from raw material to end consumer using ERC-721 or permissioned chain attestations — enabling anti-counterfeiting and ESG reporting with third-party verification.'
            },
          ].map((uc, i) => (
            <div class={`w3-usecase-card reveal stagger-${Math.min((i % 4) + 1, 4)}`}>
              <div class="uc-category">{uc.category}</div>
              <h3>{uc.title}</h3>
              <p>{uc.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- ═══════════════════════════════════════════════════
         COMPLIANCE
    ═══════════════════════════════════════════════════ -->
    <section class="w3-section bg-dark">
      <div class="w3-inner">
        <div class="compliance-grid">
          <div class="reveal-left">
            <span class="section-label">Australian Compliance</span>
            <h2 class="section-heading">Navigating the Australian Regulatory Landscape</h2>
            <p class="compliance-para">
              Australia's crypto regulatory environment is evolving rapidly. ASIC has signalled that most tokens with investment characteristics fall under existing financial product frameworks.
              The ATO treats cryptocurrency as property, with CGT events triggering on disposal, swap and DeFi yield. AUSTRAC AML/CTF obligations apply to digital currency exchange services with as-low-as-$10,000 reporting thresholds.
            </p>
            <p class="compliance-para">
              We work alongside your legal counsel to structure token offerings, DeFi protocols and payment systems that operate within current guidance — while remaining adaptable as legislation matures.
            </p>
          </div>
          <div class="compliance-cards reveal-right stagger-2">
            {[
              { body: 'ASIC', desc: 'Financial product analysis, MIS structure review, AFSL requirements', colour: '#0d9373' },
              { body: 'ATO', desc: 'Digital asset CGT treatment, DeFi tax events, SMSF crypto rules', colour: '#627eea' },
              { body: 'AUSTRAC', desc: 'DCE registration, AML/CTF program, TTRs and SMRs', colour: '#e84142' },
              { body: 'Treasury', desc: 'Token mapping framework, custody reforms, exchange licensing', colour: '#f0b90b' },
            ].map((c, i) => (
              <div class={`compliance-card reveal stagger-${i + 1}`}>
                <div class="cc-dot" style={`background:${c.colour}`}></div>
                <div class="cc-body">{c.body}</div>
                <div class="cc-desc">{c.desc}</div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>

    <!-- ═══════════════════════════════════════════════════
         SECURITY & AUDIT
    ═══════════════════════════════════════════════════ -->
    <section class="w3-section bg-surface">
      <div class="w3-inner">
        <div class="section-header reveal">
          <span class="section-label">Security First</span>
          <h2 class="section-heading">Smart Contract Security Is Non-Negotiable</h2>
          <p class="section-subheading">
            Every contract we deploy goes through a multi-layer security process. The blockchain's immutability means you don't get a patch — you need to get it right before deployment.
          </p>
        </div>
        <div class="security-grid">
          {[
            {
              step: '01',
              title: 'Threat Modelling',
              desc: 'Before writing code we model the attack surface: reentrancy, integer overflow, access control flaws, oracle manipulation, flash loan vectors and economic exploit scenarios.'
            },
            {
              step: '02',
              title: 'Automated Static Analysis',
              desc: 'Slither, Mythril and Echidna property-based fuzzing run against every contract throughout development — not just at the end. Issues are caught before they reach review.'
            },
            {
              step: '03',
              title: 'Manual Code Review',
              desc: 'Senior security engineers review all contract logic line-by-line, with particular attention to state machine correctness, privilege escalation paths and economic invariants.'
            },
            {
              step: '04',
              title: 'Independent Audit',
              desc: 'For production deployments, we coordinate external audits with leading firms (Trail of Bits, OpenZeppelin, Certik, Halborn) and implement all critical and high-severity findings before launch.'
            },
            {
              step: '05',
              title: 'Formal Verification',
              desc: 'For high-value contracts (bridges, large DeFi TVL), we apply formal verification techniques — mathematically proving that contract behaviour matches specification for all possible inputs.'
            },
            {
              step: '06',
              title: 'Emergency Controls',
              desc: 'Time locks on sensitive functions, emergency pause mechanisms, upgrade patterns (transparent proxy, UUPS) with governance-controlled upgrade authority and multisig safeguards.'
            },
          ].map((s, i) => (
            <div class={`security-card card-hover reveal stagger-${Math.min((i % 6) + 1, 6)}`}>
              <div class="sec-step">{s.step}</div>
              <h3>{s.title}</h3>
              <p>{s.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    {/* Case study section removed — previous content was fabricated. Will return with real, signed-off client work. */}

    <!-- ═══════════════════════════════════════════════════
         FAQ
    ═══════════════════════════════════════════════════ -->
    <section class="w3-section bg-surface">
      <div class="w3-inner">
        <div class="section-header reveal">
          <span class="section-label">Common Questions</span>
          <h2 class="section-heading">Web3 Development — FAQ</h2>
        </div>
        <div class="faq-list">
          {[
            {
              q: 'What does smart contract development cost?',
              a: 'A single, well-scoped smart contract (ERC-20 token, basic staking contract) typically ranges from $15,000–$40,000 including architecture, development, testing and audit coordination. A complete DeFi protocol or NFT marketplace — multiple interconnected contracts, frontend, backend indexer and security audit — typically runs $120,000–$350,000. Complex protocols with novel mechanics requiring formal verification sit above this range. We provide fixed-price proposals after a scoping workshop.'
            },
            {
              q: 'Do you conduct smart contract audits?',
              a: 'We perform internal security reviews on every contract we write, including automated analysis (Slither, Mythril, Echidna) and manual review by engineers with dedicated smart contract security experience. For production deployments, we strongly recommend — and will coordinate — an independent third-party audit with a reputable firm. We do not perform independent audits on our own code; the conflict of interest is obvious and the standard is unacceptable to us.'
            },
            {
              q: 'Which blockchain should we build on?',
              a: 'That depends on your specific requirements: transaction throughput, cost per transaction, developer ecosystem maturity, regulatory considerations, target user geography and composability with existing DeFi liquidity. Ethereum mainnet offers the strongest security guarantees and deepest liquidity but higher gas costs. L2s (Arbitrum, Base, Optimism) give near-Ethereum security with dramatically lower costs. Solana suits ultra-high-throughput consumer applications. We provide an objective recommendation after understanding your use case — not a default to whatever chain we last deployed on.'
            },
            {
              q: 'How does your work comply with Australian crypto regulations?',
              a: 'We take regulatory compliance seriously and work within the current ASIC, ATO and AUSTRAC frameworks. Our engagements include a regulatory risk assessment to identify whether your token or protocol constitutes a managed investment scheme, a financial product or a digital currency exchange service under existing law. We advise you to engage specialist crypto legal counsel (we can refer) for formal legal sign-off — we provide technical implementation that operates within the framework you agree with your lawyers.'
            },
            {
              q: 'Can you build on a private or permissioned blockchain?',
              a: 'Yes. We have experience with Hyperledger Fabric, Quorum (Go-Ethereum), Besu and permissioned EVM networks for enterprise use cases where public chain exposure is not appropriate — regulated asset registries, interbank settlement, supply chain provenance. We\'ll recommend a permissioned architecture when the use case genuinely justifies it rather than pushing clients toward public chains where it\'s not warranted.'
            },
            {
              q: 'What happens if a bug is found after deployment?',
              a: 'This is why upgrade patterns matter. Where possible, we implement upgradeable contract architectures (transparent proxy, UUPS) with time-locked governance on the upgrade authority, so critical bugs can be patched. For truly immutable contracts, we design circuit breakers and pause mechanisms that can halt operations while an upgrade contract is deployed. We also maintain emergency response playbooks with your team so the response time to a critical issue is measured in minutes, not hours.'
            },
            {
              q: 'Do you build frontends for Web3 applications?',
              a: 'Yes — full-stack Web3 development is our standard offering. We build React and Next.js frontends with ethers.js/viem/wagmi wallet integrations, The Graph or custom indexer integrations for on-chain data, IPFS/Arweave integrations for decentralised storage, and backend services where centralised components are necessary (oracle feeds, KYC/AML, off-chain order books). The smart contract is rarely the hard part — the user experience usually is.'
            },
            {
              q: 'How do you handle cross-chain token bridges securely?',
              a: 'Bridge security is one of the hardest problems in Web3 — the majority of the largest DeFi hacks have been bridge exploits. Our approach: we avoid building novel bridge mechanisms where proven solutions (LayerZero, Axelar, Wormhole, Stargate) can be integrated safely. Where custom bridging is genuinely necessary, we apply formal verification to the bridge logic, implement very conservative economic limits and deploy staged rollouts with circuit breakers that can freeze the bridge if anomalous activity is detected.'
            },
          ].map((faq, i) => (
            <div class={`faq-item reveal stagger-${Math.min(i + 1, 6)}`}>
              <button class="faq-q" data-faq={`w3-${i}`} aria-expanded="false">
                <span>{faq.q}</span>
                <svg class="faq-chevron" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
              </button>
              <div class="faq-a" id={`faq-w3-${i}`} hidden>
                <p>{faq.a}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <CTABanner />

  </main>
  <Footer />
</Layout>

<style>
  .w3-hero {
    padding: 9rem 0 6rem;
    background: #0a0f0e;
    position: relative;
    overflow: hidden;
  }

  .w3-hero-bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .w3-glow-1 {
    position: absolute;
    top: -5%;
    right: 15%;
    width: 600px;
    height: 600px;
    background: radial-gradient(ellipse, rgba(98,126,234,0.12) 0%, transparent 65%);
  }

  .w3-glow-2 {
    position: absolute;
    bottom: 0;
    left: 5%;
    width: 400px;
    height: 300px;
    background: radial-gradient(ellipse, rgba(13,147,115,0.07) 0%, transparent 70%);
  }

  .w3-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(98,126,234,0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(98,126,234,0.03) 1px, transparent 1px);
    background-size: 80px 80px;
  }

  .w3-hero-inner {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 2rem;
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr 300px;
    gap: 4rem;
    align-items: center;
  }

  .breadcrumb {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: 'DM Sans', sans-serif;
    font-size: 0.8rem;
    color: #4a6359;
    margin-bottom: 1.5rem;
  }
  .breadcrumb a { color: #4a6359; text-decoration: none; transition: color 0.2s; }
  .breadcrumb a:hover { color: #0d9373; }

  .w3-hero-title {
    font-family: 'Sora', sans-serif;
    font-size: clamp(2.5rem, 5.5vw, 4.25rem);
    font-weight: 800;
    color: white;
    line-height: 1.06;
    letter-spacing: -0.03em;
    margin-bottom: 1.5rem;
  }

  .w3-hero-sub {
    font-family: 'DM Sans', sans-serif;
    font-size: 1.125rem;
    color: #8aa09a;
    max-width: 580px;
    line-height: 1.7;
    margin-bottom: 2.5rem;
  }

  .w3-hero-ctas {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    margin-bottom: 3rem;
  }

  .hero-trust-row {
    display: flex;
    align-items: center;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .trust-item { display: flex; flex-direction: column; gap: 0.2rem; }
  .trust-num { font-family: 'Sora', sans-serif; font-size: 1.75rem; font-weight: 800; color: #0d9373; line-height: 1; }
  .trust-label { font-family: 'DM Sans', sans-serif; font-size: 0.8125rem; color: #4a6359; }
  .trust-divider { width: 1px; height: 40px; background: #1e2d28; }

  /* Hero visual */
  .w3-hero-visual {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .chain-vis {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .chain-block {
    width: 60px;
    height: 60px;
    border-radius: 10px;
    border: 1px solid rgba(13,147,115,0.3);
    background: rgba(13,147,115,0.06);
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'DM Sans', sans-serif;
    font-size: 0.7rem;
    font-weight: 600;
    color: #0d9373;
    animation: block-pulse 2s ease-in-out infinite;
  }

  .cb-1 { animation-delay: 0s; }
  .cb-2 { animation-delay: 0.3s; }
  .cb-3 { animation-delay: 0.6s; }

  @keyframes block-pulse {
    0%, 100% { opacity: 0.7; border-color: rgba(13,147,115,0.3); }
    50% { opacity: 1; border-color: rgba(13,147,115,0.7); box-shadow: 0 0 20px rgba(13,147,115,0.2); }
  }

  .chain-arrow {
    font-size: 1.25rem;
    color: rgba(13,147,115,0.4);
    font-weight: 300;
  }

  .chain-hash {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.75rem;
    color: rgba(13,147,115,0.5);
    font-variant-numeric: tabular-nums;
    letter-spacing: 0.05em;
  }

  /* Sections */
  .w3-section { padding: 7rem 0; }
  .bg-dark { background: #0a0f0e; }
  .bg-surface { background: #0f1614; }

  .w3-inner {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .section-header {
    max-width: 700px;
    margin-bottom: 3.5rem;
  }

  /* Services */
  .w3-services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
  }

  .w3-svc-card {
    background: #0a0f0e;
    border: 1px solid #1e2d28;
    border-radius: 16px;
    padding: 1.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.875rem;
  }

  .w3-svc-icon {
    width: 44px;
    height: 44px;
    border-radius: 10px;
    background: rgba(13,147,115,0.08);
    border: 1px solid rgba(13,147,115,0.15);
    display: flex;
    align-items: center;
    justify-content: center;
    color: #0d9373;
    flex-shrink: 0;
  }

  .w3-svc-card h3 { font-family: 'Sora', sans-serif; font-size: 1rem; font-weight: 600; color: white; margin: 0; }
  .w3-svc-card p { font-family: 'DM Sans', sans-serif; font-size: 0.875rem; color: #4a6359; line-height: 1.65; margin: 0; }

  /* Chains */
  .chains-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 1rem;
  }

  .chain-badge {
    background: #0f1614;
    border: 1px solid #1e2d28;
    border-radius: 14px;
    padding: 1.25rem 1.125rem;
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    transition: border-color 0.3s ease;
  }

  .chain-badge:hover { border-color: rgba(13,147,115,0.3); }

  .chain-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    margin-top: 4px;
    flex-shrink: 0;
  }

  .chain-name { font-family: 'Sora', sans-serif; font-size: 0.9375rem; font-weight: 600; color: white; }
  .chain-note { font-family: 'DM Sans', sans-serif; font-size: 0.75rem; color: #4a6359; line-height: 1.45; margin-top: 0.25rem; }

  /* Use cases */
  .usecases-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.25rem;
  }

  .w3-usecase-card {
    background: #0a0f0e;
    border: 1px solid #1e2d28;
    border-radius: 16px;
    padding: 2rem;
    transition: border-color 0.3s ease;
  }

  .w3-usecase-card:hover { border-color: rgba(13,147,115,0.3); }

  .uc-category {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.6875rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #0d9373;
    margin-bottom: 0.625rem;
  }

  .w3-usecase-card h3 { font-family: 'Sora', sans-serif; font-size: 1.0625rem; font-weight: 600; color: white; margin: 0 0 0.75rem; }
  .w3-usecase-card p { font-family: 'DM Sans', sans-serif; font-size: 0.9rem; color: #4a6359; line-height: 1.65; margin: 0; }

  /* Compliance */
  .compliance-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 5rem;
    align-items: start;
  }

  .compliance-para {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.9375rem;
    color: #4a6359;
    line-height: 1.75;
    margin: 0 0 1.25rem;
  }

  .compliance-cards {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
    margin-top: 4rem;
  }

  .compliance-card {
    background: #0a0f0e;
    border: 1px solid #1e2d28;
    border-radius: 14px;
    padding: 1.25rem;
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
  }

  .cc-dot { width: 8px; height: 8px; border-radius: 50%; margin-bottom: 0.25rem; }
  .cc-body { font-family: 'Sora', sans-serif; font-size: 1rem; font-weight: 700; color: white; }
  .cc-desc { font-family: 'DM Sans', sans-serif; font-size: 0.8125rem; color: #4a6359; line-height: 1.5; }

  /* Security */
  .security-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
  }

  .security-card {
    background: #0a0f0e;
    border: 1px solid #1e2d28;
    border-radius: 16px;
    padding: 2rem;
    display: flex;
    flex-direction: column;
    gap: 0.875rem;
  }

  .sec-step {
    font-family: 'Sora', sans-serif;
    font-size: 2rem;
    font-weight: 800;
    color: rgba(13,147,115,0.2);
    line-height: 1;
    letter-spacing: -0.04em;
  }

  .security-card h3 { font-family: 'Sora', sans-serif; font-size: 1rem; font-weight: 600; color: white; margin: 0; }
  .security-card p { font-family: 'DM Sans', sans-serif; font-size: 0.875rem; color: #4a6359; line-height: 1.65; margin: 0; }

  /* Case study */
  .casestudy-block {
    background: linear-gradient(135deg, rgba(13,147,115,0.06) 0%, #0f1614 60%);
    border: 1px dashed rgba(13,147,115,0.3);
    border-radius: 24px;
    padding: 3rem;
    max-width: 800px;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .cs-badge {
    display: inline-flex;
    align-self: flex-start;
    background: rgba(13,147,115,0.1);
    border: 1px solid rgba(13,147,115,0.25);
    color: #0d9373;
    font-family: 'DM Sans', sans-serif;
    font-size: 0.6875rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    padding: 0.25rem 0.75rem;
    border-radius: 50px;
  }

  .casestudy-block h3 { font-family: 'Sora', sans-serif; font-size: 1.375rem; font-weight: 700; color: white; margin: 0; }
  .casestudy-block p { font-family: 'DM Sans', sans-serif; font-size: 0.9375rem; color: #4a6359; line-height: 1.7; margin: 0; }

  .cs-stats {
    display: flex;
    gap: 2.5rem;
    flex-wrap: wrap;
    padding-top: 1.25rem;
    border-top: 1px solid rgba(13,147,115,0.15);
  }

  .cs-stat { display: flex; flex-direction: column; gap: 0.25rem; }
  .cs-num { font-family: 'Sora', sans-serif; font-size: 1.5rem; font-weight: 700; color: #0d9373; }
  .cs-label { font-family: 'DM Sans', sans-serif; font-size: 0.8125rem; color: #4a6359; }

  /* FAQ */
  .faq-list {
    display: flex;
    flex-direction: column;
    max-width: 900px;
  }

  .faq-item { border-bottom: 1px solid #1e2d28; }

  .faq-q {
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 1.375rem 0;
    background: none;
    border: none;
    cursor: pointer;
    text-align: left;
    font-family: 'Sora', sans-serif;
    font-size: 1rem;
    font-weight: 600;
    color: white;
    transition: color 0.2s ease;
  }

  .faq-q:hover { color: #0d9373; }
  .faq-q[aria-expanded="true"] { color: #0d9373; }

  .faq-chevron {
    flex-shrink: 0;
    transition: transform 0.3s ease;
    color: #4a6359;
  }

  .faq-q[aria-expanded="true"] .faq-chevron {
    transform: rotate(180deg);
    color: #0d9373;
  }

  .faq-a { padding-bottom: 1.375rem; }
  .faq-a p { font-family: 'DM Sans', sans-serif; font-size: 0.9375rem; color: #4a6359; line-height: 1.75; margin: 0; }

  /* Responsive */
  @media (max-width: 1100px) {
    .w3-hero-inner { grid-template-columns: 1fr; }
    .w3-hero-visual { display: none; }
    .w3-services-grid { grid-template-columns: repeat(2, 1fr); }
    .chains-grid { grid-template-columns: repeat(3, 1fr); }
    .security-grid { grid-template-columns: repeat(2, 1fr); }
    .compliance-grid { grid-template-columns: 1fr; }
    .compliance-cards { margin-top: 0; }
  }

  @media (max-width: 640px) {
    .w3-services-grid,
    .usecases-grid,
    .security-grid { grid-template-columns: 1fr; }
    .chains-grid { grid-template-columns: repeat(2, 1fr); }
    .compliance-cards { grid-template-columns: 1fr; }
    .hero-trust-row { gap: 1.25rem; }
    .trust-divider { display: none; }
  }
</style>

<script>
  document.querySelectorAll<HTMLButtonElement>('.faq-q').forEach((btn) => {
    btn.addEventListener('click', () => {
      const isOpen = btn.getAttribute('aria-expanded') === 'true';
      const idx = btn.dataset.faq;
      const panel = document.getElementById(`faq-${idx}`);
      btn.setAttribute('aria-expanded', String(!isOpen));
      if (panel) panel.hidden = isOpen;
    });
  });
</script>
